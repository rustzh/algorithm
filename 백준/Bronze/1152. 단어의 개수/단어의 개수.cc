#include <bits/stdc++.h>
using namespace std;

#define mx 1000000

int main()
{
   ios_base::sync_with_stdio(false);cin.tie(NULL);cout.tie(NULL);   

   int cnt = 0;
   string s;
   
   while(cin >> s) {
   	cnt ++;
   }

   cout << cnt;

   return 0;
}